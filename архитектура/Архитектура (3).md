## Структура приложения

```
capillaroscope_app/
    domain/
        models.py
        states.py
        protocols.py

    application/
        device_controller.py
        camera_controller.py
        preview_service.py
        autofocus_service.py
        measurement_service.py
        processing_service.py

    hardware/
        capillaroscope_device_base.py
        serial_capillaroscope_device.py
        camera_base.py
        vendor_camera.py
        mock_camera.py
        mock_device.py

    processing/
        vessel_segmentation.py
        oximetry.py
        image_quality.py

    storage/
        measurement_repository.py
        image_writer.py
        config_store.py

    ui/
        main_window.py
        preview_panel.py
        connection_panel.py
        camera_panel.py
        led_panel.py
        focus_panel.py
        measurement_panel.py
        processing_panel.py

    app.py
```

## Потоки

```
GUI thread
    интерфейс

Camera worker
    получение кадров / preview

Device worker
    команды МК, статусы

Processing worker
    сегментация, расчет метрик

Storage worker
    сохранение кадров и мета
```
## Описание классов

### models

```
LedChannel
	name
	wavelength_nm
	descsription
	
LedSettings
	channel
	duty_cycle
	enabled
	
CameraSettings
	exposure_ms
	fps
	resolution
	roi
	gain
	
DeviceStatus
	device_id
	firmware_version
	is_connected
	led_570_enabled
	led_600_enabled
	focus_position
	limit_switch_min
	limit_switch_max
	has_errors
	error_message
	ready_for_capture
	
CameraStatus
	is_connected
	is_preview_active
	camera_name
	last_frame_timestamp
	has_errors
	error_message

FocusState
	position
	is_moving
	is_focused
	focus_scsore
	
Frame
	image
	timestamp
	camera_settings
	led_channel
	metadata

MeasurementPair
	measurement_id
	frame_570
	frame_600
	device_status
	camera_settings
	led_settings
	focus_position
	roi
	timestamp
	
ProcessingResult
	measurement_id
	vessel_mask
	quality_score
	oxygenation_map
	summary_metrics
	warning
	timestamp
```

### states

```
DISCONNECTED
DEVICE_CONNECTED
CAMERA_CONNECTED
IDLE
PREVIEW
FOCUSING
READY_TO_CAPTURE
CAPTURING
PROCESSING
ERROR
```

### hardware

```
CameraBase
    connect()
    disconnect()
    start_preview()
    stop_preview()
    capture_frame()
    set_exposure()
    set_resolution()
    get_status()

CapillaroscopeDeviceBase
    connect()
    disconnect()
    get_status()
    set_led_duty(channel, value)
    enable_led(channel)
    disable_led(channel)
    move_focus(steps)
    stop_focus()
    execute_frame_command(...)
```